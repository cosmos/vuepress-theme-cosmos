var fs = require("fs");

module.exports = (options, ctx) => {
  // const versions = fs
  //   .readFileSync("./versions", { encoding: "utf8" })
  //   .split("\n")
  //   .filter(version => version != "");
  // Object.assign(
  //   options,
  //   Object.assign({
  //     versions
  //   })
  // );
  // return options;
};
